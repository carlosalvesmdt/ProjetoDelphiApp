unit TestuntFunctionTarefas2;
{

  Delphi DUnit Test Case
  ----------------------
  This unit contains a skeleton test case class generated by the Test Case Wizard.
  Modify the generated code to correctly setup and call the methods from the unit 
  being tested.

}

interface

uses
  TestFramework, Vcl.ComCtrls, untFunctionTarefas2, untProcessoThread;

type
  // Test methods for class TClassTarefas2

  TestTClassTarefas2 = class(TTestCase)
  strict private
    FClassTarefas2: TClassTarefas2;
  public
    procedure SetUp; override;
    procedure TearDown; override;
  published
    procedure TestExecutarThread1;
    procedure TestExecutarThread2;
  end;

implementation

procedure TestTClassTarefas2.SetUp;
begin
  FClassTarefas2 := TClassTarefas2.Create;
end;

procedure TestTClassTarefas2.TearDown;
begin
  FClassTarefas2.Free;
  FClassTarefas2 := nil;
end;

procedure TestTClassTarefas2.TestExecutarThread1;
var
  pMiliSegundos: Integer;
  pThread: TProgressBar;
begin
  // TODO: Setup method call parameters
  pThread:= TProgressBar.Create(nil);
  pMiliSegundos := 1;
  FClassTarefas2.ExecutarThread1(pThread, pMiliSegundos);
  // TODO: Validate method results
  CheckException(FClassTarefas2.ExecutarThread1(pThread, pMiliSegundos),nil,'Ocorreu um erro na rotina ExecutarThread1');
end;

procedure TestTClassTarefas2.TestExecutarThread2;
var
  pMiliSegundos: Integer;
  pThread: TProgressBar;
begin
  // TODO: Setup method call parameters
  pThread:= TProgressBar.Create(nil);
  pMiliSegundos := 1;
  CheckException(FClassTarefas2.ExecutarThread2(pThread, pMiliSegundos),nil,'Ocorreu um erro na rotina ExecutarThread2');

end;

initialization
  // Register any test cases with the test runner
  RegisterTest(TestTClassTarefas2.Suite);
end.

