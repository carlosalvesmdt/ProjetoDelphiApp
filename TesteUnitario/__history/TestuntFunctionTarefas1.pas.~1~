unit TestuntFunctionTarefas1;
{

  Delphi DUnit Test Case
  ----------------------
  This unit contains a skeleton test case class generated by the Test Case Wizard.
  Modify the generated code to correctly setup and call the methods from the unit 
  being tested.

}

interface

uses
  TestFramework, System.Classes, uspQuery, untFunctionTarefas1;

type
  // Test methods for class TClassTarefas1

  TestTClassTarefas1 = class(TTestCase)
  strict private
    FClassTarefas1: TClassTarefas1;
  public
    procedure SetUp; override;
    procedure TearDown; override;
  published
    procedure TestGerarSql;
  end;

implementation

procedure TestTClassTarefas1.SetUp;
begin
  FClassTarefas1 := TClassTarefas1.Create;
end;

procedure TestTClassTarefas1.TearDown;
begin
  FClassTarefas1.Free;
  FClassTarefas1 := nil;
end;

procedure TestTClassTarefas1.TestGerarSql;
var
  ReturnValue: string;
  pSpQuery: TspQuery;
begin
  // TODO: Setup method call parameters
  pSpQuery := pSpQuery.Create(nil);
  pSpQuery.spColunas.Add('A');
  pSpQuery.spColunas.Add('B');
  pSpQuery.spCondicoes.Add('A = 1');
  pSpQuery.spTabelas.Add('TESTE');
  ReturnValue := FClassTarefas1.GerarSql(pSpQuery);
  CheckEquals('SELECT A,B FROM TESTE WHERE A = 1',ReturnValue,'Ocorreu um erro na Rotina GerarSql');
  // TODO: Validate method results
end;

initialization
  // Register any test cases with the test runner
  RegisterTest(TestTClassTarefas1.Suite);
end.

